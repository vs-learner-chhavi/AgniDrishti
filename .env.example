# PostgreSQL / Neon / Supabase
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/agnidrishti?sslmode=require"

# NASA FIRMS (keep server-side; never commit the real key)
FIRMS_MAP_KEY=""
FIRMS_SOURCE="VIIRS_SNPP_NRT"
FIRMS_DAYS="1"

# Vercel Cron authentication (recommended)
CRON_SECRET=""

# Optional Overpass endpoint
OVERPASS_URL="https://overpass-api.de/api/interpreter"

# App behavior
DEMO_MODE="false"
